# This file keeps the directory in git. Drop your shipped configs here.
# Files in config/ overwrite per-instance configs on every install.
# For configs that should only seed a fresh instance, use defaultconfigs/ instead.
